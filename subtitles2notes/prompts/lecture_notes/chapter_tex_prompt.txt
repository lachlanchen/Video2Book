You are writing the first-pass LaTeX draft for a Leonard Susskind lecture-note chapter.

Full task context:
$task_context

Primary objective:
- produce polished, mathematically engaging notes that remain faithful to the lecture transcript and extracted frames
- keep the lecture's narrative rhythm, including the order in which concepts are introduced and the way the lecturer motivates each new equation or construction

Lecture metadata:
- Course: $course_title
- Course descriptor: $course_descriptor
- Lecture: $lecture_title
- Lecture number: $lecture_number
- Transcript path: $transcript_rel
- Video path: $video_rel
- Available asset basenames:
$asset_list

Reference book excerpts:

$book_reference_text

Figure reading notes:

$figures_markdown_text

Writing rules:
- output raw LaTeX only
- no markdown
- no code fences
- start with `\chapter{...}`
- do not include `\documentclass`, package imports, `\begin{document}`, `\end{document}`, bibliography commands, or a table of contents
- use only common LaTeX constructs already loaded by the preamble:
  - `\chapter`, `\section`, `\subsection`, `\paragraph`
  - `equation`, `align`, `figure`, `table`, `itemize`, `enumerate`
  - `definition`, `theorem`, `proposition`, `remark`, `proof`
  - `tikzpicture`
- if you include graphics, use only the listed asset basenames
- keep an extracted screenshot only when it provides useful, correctly timed blackboard evidence
- if you redraw a diagram or equation in TikZ, keep the screenshot nearby only when each version adds distinct value
- if an exact board equation is uncertain, either:
  - present the canonical standard form with a brief textual caveat, or
  - stay conceptual instead of hallucinating detail
- use the Susskind-authored reference excerpts for notation, pacing, and canonical mathematical framing when they align with the lecture
- do not copy long passages from the reference text
- do not mention being an AI, attached images, or prompt instructions
- include only mathematical structure motivated by the lecture or needed to make its argument coherent
- read the transcript as a narrated lecture, not just a bag of facts; preserve the speaker's progression, setup, and payoff
- use direct, restrained explanatory prose; use first-person plural only when it naturally guides an argument
- draw on the lecturer's clarity without impersonating him, repeatedly narrating his actions, or inventing his intentions
- never mention curation, tooling, AI, agents, prompts, LazyingArt LLC, or Video2Book in chapter prose
- for every included figure, describe the physics in the caption and put its video time in `\lectureframe{HH:MM:SS}` immediately after the caption; reserve `\lecturetimestamp` for `classroomqa`
- preserve a classroom exchange only when the transcript supports both the audience question and response; use `classroomqa`, `\audiencequestion`, `\lecturerresponse`, and `\lecturetimestamp`
- use `\editorialnote{...}` for a necessary nontrivial editorial clarification or reconstruction
- center figure environments, screenshots, TikZ diagrams, and standalone visual reconstructions unless there is a specific local reason not to
- when you create a flowchart, diagram, or table, design it for later pocket-size export:
  - wrap long node labels manually with line breaks
  - prefer narrow vertical layouts over wide horizontal sprawl
  - keep the final TikZ picture or tabular block within `\linewidth`
  - if there is a tradeoff, slightly larger readable labels are better than a wide overflowing figure
- avoid giant matrix-style flowcharts or overly wide tables unless the lecture absolutely requires them

Content requirements:
- give the chapter a specific descriptive title rather than only a course name or lecture number
- let the lecture determine the number and length of sections, equations, examples, and figures
- do not force an overview, summary, worked derivation, question block, or visual quota
- preserve useful repetition when it helps the reader digest an idea
- keep the order of sections close to the transcript unless a local merge or split materially improves comprehension

Chapter plan:

$analysis_text

Figure reading notes:

$figures_markdown_text

Visual extraction notes:

$visual_notes_text

Narrative map:

$narrative_map_text

Math bank:

$math_bank_text

Transcript:

$transcript_text
