You are refining a LaTeX lecture-note chapter for a Leonard Susskind lecture.

Full task context:
$task_context

Primary objective:
- turn the draft into a smoother, more mathematically coherent chapter
- use the transcript, chapter plan, and visual extraction notes to improve fidelity and clarity
- preserve the lecture's narration and rhythm while tightening the prose

Lecture metadata:
- Course: $course_title
- Course descriptor: $course_descriptor
- Lecture: $lecture_title
- Lecture number: $lecture_number
- Transcript path: $transcript_rel
- Video path: $video_rel
- Available asset basenames:
$asset_list

Rules:
- output raw LaTeX only
- no markdown
- no code fences
- start with `\chapter{...}`
- do not include `\documentclass`, package imports, `\begin{document}`, `\end{document}`, bibliography commands, or a table of contents
- preserve the strongest mathematical content from the draft
- correct notation inconsistencies, weak transitions, and rough exposition
- keep extracted screenshots only when they provide useful, correctly timed blackboard evidence
- if a figure is better redrawn, retain the original screenshot only when each version adds distinct value
- keep visible equations faithful when legible; when not legible, use a cautious standard reconstruction and state assumptions briefly in prose
- do not mention being an AI, attached images, or prompt instructions
- check the draft against the transcript order and restore missing motivational beats or transition sentences when the draft became too textbook-like
- use direct, restrained explanatory prose and first-person plural only when it naturally guides an argument
- draw on the lecturer's clarity without impersonating him, repeatedly narrating his actions, or inventing his intentions
- never mention curation, tooling, AI, agents, prompts, LazyingArt LLC, or Video2Book in chapter prose

Required improvements:
- make the chapter read as deliberate notes rather than transcript fragments
- retain or improve the worked mathematics
- integrate equation and diagram evidence from the frame analysis
- retain screenshot evidence only when it is relevant, legible, correctly timed, and not redundant with the interpretation
- keep the tone serious, smooth, and mathematically engaging
- preserve every transcript-confirmed audience exchange with `classroomqa`, `\audiencequestion`, `\lecturerresponse`, and `\lecturetimestamp`; never synthesize dialogue from ordinary exposition
- let the lecture determine whether a summary is useful instead of adding one by default
- preserve useful repetition and the lecture's conceptual order rather than optimizing only for compression
- remove selected screenshots that are irrelevant, mistimed, unreadable, or redundant
- use `\editorialnote{...}` for necessary nontrivial reconstruction and keep figure captions focused on physics
- put a retained video's frame time in `\lectureframe{HH:MM:SS}` immediately after its caption; never use `\lecturetimestamp` outside `classroomqa` or inside `\editorialnote`
- keep figures, TikZ diagrams, and standalone reconstructed equations centered unless an asymmetric layout is genuinely better
- make any flowchart, diagram, or table robust for later pocket-size export:
  - wrap long labels
  - compress width before compressing readability
  - prefer taller/narrower layouts over sprawling wide ones
  - keep diagrams and tables within `\linewidth`
  - if needed, accept slightly larger local text and a taller figure rather than a wide overflowing one

Chapter plan:

$analysis_text

Figure reading notes:

$figures_markdown_text

Visual extraction notes:

$visual_notes_text

Narrative map:

$narrative_map_text

Math bank:

$math_bank_text

Current draft:

$draft_tex

Transcript:

$transcript_text
